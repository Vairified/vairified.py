The OAuth value objects `OAuthConfig`, `AuthorizationResponse`, and `TokenResponse` are now immutable (`@dataclass(frozen=True)`), matching the TS SDK's `readonly` models and the `sdk.md` immutability convention. Attempting to mutate one after construction now raises `FrozenInstanceError`.
